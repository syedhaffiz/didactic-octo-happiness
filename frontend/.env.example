# Copy to `.env.local` and fill in. Vite picks up `.env.local` automatically.

# --- API ---------------------------------------------------------------
# Where the backend lives. Defaults to localhost:4000 if unset.
VITE_API_BASE_URL=http://localhost:4000/api

# --- Data source -------------------------------------------------------
# `true` → frontend serves Inventory data from mocks (see src/mocks/).
# `false` → frontend hits the backend.
VITE_USE_MOCK_DATA=true

# --- Auth --------------------------------------------------------------
# This app does NOT authenticate. The host owns SSO and the remote consumes
# the host's token (registered at runtime via Module Federation). No Azure
# config is needed here. Standalone `npm run dev` runs token-less (mock data).
