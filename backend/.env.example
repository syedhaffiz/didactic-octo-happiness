# Server
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Data source: "mock" today; "databricks" later (Phase 5).
DATA_SOURCE=mock

# Databricks (placeholders — not used while DATA_SOURCE=mock)
DATABRICKS_HOST=
DATABRICKS_HTTP_PATH=
DATABRICKS_TOKEN=
DATABRICKS_CATALOG=
DATABRICKS_SCHEMA=
